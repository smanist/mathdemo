# Documentation Skeleton

Build locally with:

```bash
sphinx-build -b html docs docs/_build/html
```

Then open `docs/_build/html/index.html`.

Interactive examples are initialized by `docs/_static/js/course-interactives.js`.
The placeholder pages show the intended Markdown patterns for cross-references,
equations, and inline interactive examples.
